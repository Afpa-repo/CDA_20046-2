{% extends 'base.html.twig' %}

{% block title %}S'inscrire !{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    {{ include('user/_form.html.twig') }}

    <button class="btn">{{ button_label|default('Save', {'id': 1}) }}</button>

    <a href="{{ path('user_index') }}">Retour à la liste</a>
{% endblock %}
