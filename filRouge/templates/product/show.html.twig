{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
<div class="container">
    <h1>Product</h1>

<div class="row">
<div class="col">
{# Emplacement photo #}
<div class="card text-center col-6">
  <div class="card-header">
    Photo 01
  </div>
  <div class="card-body">
    <h4 class="card-title">Visuel Principal</h4>
    <p class="card-text">Image en situation.</p>
    <a href="#!" class="btn btn-dark">Link</a>
  </div>
</div>
</div>

<div class="col">
    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr> #}
            <tr>
                <th>ProName</th>
                <td>{{ product.proName }}</td>
            </tr>
            {# <tr>
                <th>ProStockAle</th>
                <td>{{ product.proStockAle }}</td>
            </tr> #}
    {#        <tr>
                <th>ProUnitPrice</th>
                <td>{{ product.proUnitPrice }}</td>
            </tr>#}
            {# <tr>
                <th>ProUnitStockPhy</th>
                <td>{{ product.ProUnitStockPhy }}</td>
            </tr>
            <tr>
                <th>ProUnitOnOrder</th>
                <td>{{ product.proUnitOnOrder }}</td>
            </tr>
            <tr>
                <th>ProDiscontinued</th>
                <td>{{ product.proDiscontinued }}</td>
            </tr> #}

            <tr>
                <th>Format</th>
                    <td>
                    <div class="btn-group btn-group-toggle" data-toggle="buttons">
                        <label class="btn btn-outline-light">
                            <input type="checkbox" checked="" autocomplete="off"> A3
                        </label>
                        <label class="btn btn-outline-light active">
                            <input type="checkbox" autocomplete="off"> A2
                        </label>
                        <label class="btn btn-outline-light active">
                            <input type="checkbox" autocomplete="off"> A1
                        </label>
                    </div>
                    </td>
            </tr>

            <tr>
                <th>ProLib</th>
                <td>{{ product.proLib }}</td>
            </tr>
            <tr>
                <th>ProDescription</th>
                <td>{{ product.proDescription }}</td>
            </tr>
            <tr>
                <th>ProNote</th>
                <td>{{ product.proNote }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('product_index') }}">back to list</a>

    <a href="{{ path('product_edit', {'id': product.id}) }}">edit</a>
    <a href="{{ path('cart_add', {'id': product.id}) }}">ajouté au panier</a>

    {{ include('product/_delete_form.html.twig') }}
    </div> 
   </div> 
</div>

{% endblock %}
