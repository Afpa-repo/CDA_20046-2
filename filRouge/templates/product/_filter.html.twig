{{ form_start(form) }}

    {{ form_row(form.recherche)}}

    <h4>Thèmes</h4>
    {{ form_row(form.themes) }}

    <h4>Promotions</h4>
    {{ form_row(form.promo) }}

<button type="submit" class="btn btn-primary w-75 mb-4 mt-4">Filtrer</button>
{{ form_end(form) }}