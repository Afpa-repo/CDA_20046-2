<div
	class="card">
	<img class="img-fluid w-60 mt-4" src="{{ asset ("images/index/catalogue@2x.png") }}" alt="">
	<div
		class="card-body">
		<h5 class="card-title">{{ product.ProName}}</h5>
		<div class="card-title">
			{# {{ product.ProPrice }} #}
			{# {% if product.promo %} #}
        {# {% if product.promo %}
				<div class="text text-warning"> Promo !</div>
		{% endif %} #}
	</div>
	<p class="text text-muted">{{ product.ProDescription|striptags }}</p>
</div>
{% for c in product.theme %}
	<em class="text-muted text-primary"></em>
{% endfor %}</div></div></div>
